#include <time.h>
long	GetTime(char *pgmtime,int timesize)
{
	char *pgmtime_ptr=pgmtime;

	struct tm *tmstruct;
	time_t	tnow;

	time(&tnow);
	tmstruct=localtime(&tnow);
	strftime(pgmtime_ptr,timesize,"%m/%d/%Y-%H:%M:%S",tmstruct);
	return(tnow);

}
